specVersion: "v1beta"
serviceId: "twisenight"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "twisenight-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
